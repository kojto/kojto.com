<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <!-- Scheduled Action to Sync Hourly Reports from MySQL every 30 minutes -->
        <record id="ir_cron_sync_hourly_reports" model="ir.cron">
            <field name="name">Sync Hourly Reports from MySQL (Every 30 min)</field>
            <field name="model_id" ref="model_kojto_energy_management_power_meter_readings"/>
            <field name="state">code</field>
            <field name="code">model.cron_sync_hourly_reports()</field>
            <field name="interval_number">30</field>
            <field name="interval_type">minutes</field>
            <field name="active" eval="True"/>
            <field name="nextcall" eval="(datetime.now() + timedelta(minutes=30))"/>
        </record>
    </data>
</odoo>
