<odoo>
    <data>
        <!-- List View for Kojto Assets Value -->
        <record model="ir.ui.view" id="view_kojto_asset_value_list">
            <field name="name">kojto.asset.value.list</field>
            <field name="model">kojto.asset.value</field>
            <field name="arch" type="xml">
                <list class="ko-list-main view_kojto_asset_value_list" limit="100" sample="1">
                    <field name="asset_id" />
                    <field name="currency_id" />
                    <field name="datetime_start" />
                    <field name="value" />
                    <field name="comment" />
                </list>
            </field>
        </record>
        <!-- Form View for Kojto Assets Value -->
        <record model="ir.ui.view" id="view_kojto_asset_value_form">
            <field name="name">kojto.asset.value.form</field>
            <field name="model">kojto.asset.value</field>
            <field name="arch" type="xml">
                <form class="ko-form-main view_kojto_asset_value_form">
                    <sheet class="ko-form-body">
                        <!-- Custom Header -->
                        <div class="d-flex justify-content-between align-items-center mb-4">
                            <!-- Left Section -->
                            <div class="d-flex align-items-center">
                                <div class="ko-form-view-title">
                                    <span>ASSET VALUE</span>
                                </div>
                            </div>
                        </div>
                        <group class="col-lg-6">
                            <field name="asset_id" />
                            <field name="currency_id" />
                            <field name="datetime_start" />
                            <field name="value" />
                            <field name="comment" />
                            <field name="create_date" readonly="1" />
                            <field name="write_date" readonly="1" />
                        </group>
                    </sheet>
                </form>
            </field>
        </record>
        <!-- Search View for Kojto Assets Value -->
        <record model="ir.ui.view" id="view_kojto_asset_value_search">
            <field name="name">kojto.asset.value.search</field>
            <field name="model">kojto.asset.value</field>
            <field name="arch" type="xml">
                <search string="Asset Value">
                    <field name="asset_id" />
                    <field name="currency_id" />
                    <field name="datetime_start" />
                    <field name="value" />
                    <filter string="Asset" name="groupby_asset_id" context="{'group_by': 'asset_id'}" />
                    <filter string="Currency" name="groupby_currency_id" context="{'group_by': 'currency_id'}" />
                </search>
            </field>
        </record>
        <!-- Action to Open List and Form Views for Kojto Assets Value -->
        <record model="ir.actions.act_window" id="action_kojto_asset_value">
            <field name="name">Asset Value</field>
            <field name="type">ir.actions.act_window</field>
            <field name="res_model">kojto.asset.value</field>
            <field name="view_mode">list,form</field>
            <field name="context">{}</field>
            <field name="help" type="html">
                <p class="o_view_nocontent_smiling_face">Create your asset value records here</p>
            </field>
        </record>
    </data>
</odoo>
